import { Config } from '../Config';
import { Agrippa } from '../utils';
import { PipelineContext } from '.';

export const initializeContext = (config: Partial<Config>, loadedFiles: Record<string, Agrippa.File | null | undefined>): PipelineContext => {
  
  
};